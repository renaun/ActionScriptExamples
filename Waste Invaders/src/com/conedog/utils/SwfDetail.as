/*	BLOC Lib for ActionScript 3.0	Copyright (c) 2009, The Bloc Development Team*/package com.conedog.utils {	import flash.display.DisplayObject;	import flash.system.Capabilities;	/*	A simple set of functions to get information on the swf location				@author Mat Groves		@version 04/01/09	*/	public class SwfDetail 	{		// E V E N T S --------------------------------------------//		// P R O P E R T I E S ------------------------------------//		// G E T T E R S / S E T T E R S --------------------------//				/*			Determens if the site is running of a Mac Computer		*/		public static function get isMac():Boolean		{			return Capabilities.version.substr(0,3) == "MAC";		}				/*			Determens if the site is running of a Windows Computer		*/		public static function get isWin():Boolean		{			return Capabilities.version.substr(0,3) == "WIN";		}				/*			Determens if the site is running of a UNIX Computer		*/		public static function get isUnix():Boolean		{			return Capabilities.version.substr(0,4) == "UNIX";		}						/*			Determens if the site is running from the Web Player		*/		public static function get isWeb():Boolean 		{			return Capabilities.playerType == 'PlugIn' || Capabilities.playerType == 'ActiveX';		}				/*			Determens if the site is being run from the Flash IDE Player		*/		public static function get isIde():Boolean 		{			return Capabilities.playerType == 'External';		}				/*			Determens if the site is being run from the Stand Alone Player		*/		public static function get isStandAlone():Boolean 		{			return Capabilities.playerType == 'StandAlone';		}						// C O N S T R U C T O R ----------------------------------//		// P U B L I C --------------------------------------------//						public static function isDomain(o:DisplayObject, domain:String):Boolean 		{			return SwfDetail.getDomain(o).slice(-domain.length) == domain;		}		public static function getDomain(o:DisplayObject):String 		{			var baseUrl:String = String(o.loaderInfo.url.split('://')[1]).split('/')[0];			return (baseUrl.substr(0, 4) == 'www.') ? baseUrl.substr(4) : baseUrl;		}				// P R I V A T E ------------------------------------------//		// H A N D L E R S ----------------------------------------//					}}